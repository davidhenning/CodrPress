- extends 'layout.haml'
- set post = posts.first()
- block title
  #{post.title} #{parent()}

- block body
  %section.articles
    - include 'articles.haml' with {'page': true}
  - if post.disqus
    %section.comments
      %h3 Comments